{
  "dependencies": {
    "@floating-ui/dom": "^1.6.13"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  },
  "scripts": {
    "build": "esbuild --bundle content_src.js --outfile=content.js --minify --sourcemap",
    "watch": "esbuild --bundle src/content.js --outfile=dist/content.js --minify --sourcemap --watch"
  }
}