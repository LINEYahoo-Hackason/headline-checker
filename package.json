{
  "dependencies": {
    "@floating-ui/dom": "^1.6.13"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  },
  "scripts": {
    "build": "esbuild --bundle content_src.js --outfile=content.js --minify --sourcemap --target=es2023,chrome134 --platform=browser",
    "watch": "npm run build -- --watch"
  }
}